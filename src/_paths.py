"""Canonical path definitions for kr-company-registry."""

from pathlib import Path

PROJECT_ROOT = Path(__file__).parent.parent
DATA_RAW_DART = PROJECT_ROOT / "data" / "raw" / "dart"
DATA_DIST = PROJECT_ROOT / "data" / "dist"
